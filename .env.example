# Tripistry - environment configuration template.
#
# Copy this file to ".env" and fill in your own database credentials.
# The .env file is ignored by git and must NEVER be committed.
#
# For a default XAMPP install, DB_USER is "root" and DB_PASSWORD is empty.

DB_HOST=localhost
DB_NAME=u24611400_Tripistry
DB_USER=root
DB_PASSWORD=
